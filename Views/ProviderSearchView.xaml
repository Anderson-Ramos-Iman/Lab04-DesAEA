<UserControl x:Class="Semana04NeptunoWpf.Views.ProviderSearchView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Grid>
        <Grid.RowDefinitions><RowDefinition Height="Auto"/><RowDefinition Height="Auto"/><RowDefinition Height="*"/><RowDefinition Height="Auto"/></Grid.RowDefinitions>
        <StackPanel Margin="0,0,0,20">
            <TextBlock Text="Buscar proveedores" FontSize="20" FontWeight="SemiBold"/>
            <TextBlock Text="Complete uno o ambos criterios. La búsqueda usa nombre de contacto y ciudad." FontSize="13" Foreground="#68706A" Margin="0,5,0,0"/>
        </StackPanel>
        <Grid Grid.Row="1" Margin="0,0,0,20">
            <Grid.ColumnDefinitions><ColumnDefinition Width="*"/><ColumnDefinition Width="*"/><ColumnDefinition Width="Auto"/></Grid.ColumnDefinitions>
            <StackPanel Margin="0,0,12,0">
                <TextBlock Text="Nombre de contacto" FontSize="12" Foreground="#68706A" Margin="2,0,0,5"/>
                <TextBox x:Name="ContactTextBox" Height="42" FontSize="14" Padding="12,8" VerticalContentAlignment="Center"/>
            </StackPanel>
            <StackPanel Grid.Column="1" Margin="0,0,12,0">
                <TextBlock Text="Ciudad" FontSize="12" Foreground="#68706A" Margin="2,0,0,5"/>
                <TextBox x:Name="CityTextBox" Height="42" FontSize="14" Padding="12,8" VerticalContentAlignment="Center"/>
            </StackPanel>
            <Button Grid.Column="2" Content="Buscar" Click="SearchButton_Click" Background="#B7E800" BorderBrush="#B7E800" FontWeight="SemiBold" Padding="24,10" VerticalAlignment="Bottom"/>
        </Grid>
        <DataGrid x:Name="ProvidersDataGrid" Grid.Row="2" AutoGenerateColumns="False" IsReadOnly="True" CanUserAddRows="False" GridLinesVisibility="Horizontal" RowHeight="36" FontSize="14">
            <DataGrid.Columns>
                <DataGridTextColumn Header="ID" Binding="{Binding IdProveedor}" Width="60"/>
                <DataGridTextColumn Header="Compañía" Binding="{Binding NombreCompania}" Width="*"/>
                <DataGridTextColumn Header="Contacto" Binding="{Binding NombreContacto}" Width="150"/>
                <DataGridTextColumn Header="Cargo" Binding="{Binding CargoContacto}" Width="140"/>
                <DataGridTextColumn Header="Ciudad" Binding="{Binding Ciudad}" Width="110"/>
                <DataGridTextColumn Header="País" Binding="{Binding Pais}" Width="100"/>
                <DataGridTextColumn Header="Teléfono" Binding="{Binding Telefono}" Width="125"/>
            </DataGrid.Columns>
        </DataGrid>
        <TextBlock x:Name="StatusText" Grid.Row="3" Text="Ejemplo: Charlotte y Londres." FontSize="13" Foreground="#68706A" Margin="0,18,0,0"/>
    </Grid>
</UserControl>
